package com.user.springbootcrudrest;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Mark the class as a Spring Boot application.
@SpringBootApplication
public class SpringbootCrudRestApplication {
	public static void main(String[] args) {
		// Run the Spring application by passing the class and command-line arguments to SpringApplication.run().
		SpringApplication.run(SpringbootCrudRestApplication.class, args);
	}
}
